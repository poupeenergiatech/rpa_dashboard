# Copie para .env e preencha com os valores reais.

# Credenciais de login em https://qrcode.allpfit.com.br/
ALLPFIT_EMAIL=
ALLPFIT_PASSWORD=

# Segredo compartilhado com o backend vendedores-dashboard-allpfit para
# autenticar o POST em /api/webhooks/scans (header Authorization: Bearer <secret>).
# Independente de AGREGADOR_WEBHOOK_SECRET -- não reusar o mesmo valor.
SCANS_WEBHOOK_SECRET=

# URL de destino do webhook.
# Produção: https://vendedores-dashboard-allpfit.gygwt3.easypanel.host/api/webhooks/scans
# Teste local: http://localhost:8787/api/webhooks/scans
SCANS_WEBHOOK_URL=
